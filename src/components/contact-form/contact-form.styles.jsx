import styled from 'styled-components'

export const Form = styled.form`
    width: 100%;
    max-width: 46rem;
    margin: clamp(2rem, 30vw, 3vw) auto;

    button {
        width: 100%;
        margin-top: 4rem;
        font-size: 2rem;
        font-weight: 500;
    }
` 